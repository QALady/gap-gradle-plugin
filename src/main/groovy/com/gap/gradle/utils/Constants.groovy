package com.gap.gradle.utils

class Constants {

  final ARTIFACTORY_HOME = "http://artifactory.gapinc.dev/artifactory"
  final ARTIFACTORY_REMOTE_REPOS = "${ARTIFACTORY_HOME}/remote-repos"
  final ARTIFACTORY_LOCAL_NON_PROD = "${ARTIFACTORY_HOME}/local-non-prod"
}
